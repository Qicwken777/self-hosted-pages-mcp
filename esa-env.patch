diff --git a/entry.mjs b/entry.mjs
index d53f950..2c97df4 100644
--- a/entry.mjs
+++ b/entry.mjs
@@ -3,8 +3,8 @@
  *
  * 前置：
  *   1. ESA 控制台 -> 边缘计算和 AI -> KV 存储，创建存储空间，名字为 pages
- *   2. 给函数/Pages 配环境变量 MCP_AUTH_TOKEN（或直接改下面 SECRET）
- *   3. esa.jsonc 里 "entry": "./entry.mjs"，路由把 /mcp-server、/kv、/share/* 指向本函数
+ *   2. 函数详情 -> 基本信息 -> 函数变量，添加 MCP_AUTH_TOKEN（加密存储）
+ *   3. esa.jsonc 里 "entry": "./entry.mjs"，路由把全部路径指向本函数
  *
  * 函数入口约定：export default { async fetch(request) {} }
  * KV 用官方 EdgeKV：new EdgeKV({ namespace: "pages" })
@@ -12,10 +12,6 @@
 
 // ---------- 配置 ----------
 const NAMESPACE = "pages";
-// 建议从环境变量读；若 ES 环境变量取不到的区，直接把 token 填这里兜底
-const SECRET = (typeof process !== "undefined" && process.env && process.env.MCP_AUTH_TOKEN)
-  ? process.env.MCP_AUTH_TOKEN
-  : "change-me-strong-random-token";
 
 // ---------- 响应头 ----------
 const CORS = { "Access-Control-Allow-Origin": "*" };
@@ -39,7 +35,7 @@ function rpcError(id, code, message) {
 }
 
 // ---------- 主处理 ----------
-async function handleRequest(request) {
+async function handleRequest(request, token) {
   const url = new URL(request.url);
   const { pathname } = url;
 
@@ -57,7 +53,7 @@ async function handleRequest(request) {
 
   // 鉴权：仅写接口要求 token，share / 首页放行
   const isProtected = pathname.startsWith("/mcp-server") || pathname.startsWith("/kv");
-  if (isProtected && request.headers.get("Authorization") !== `Bearer ${SECRET}`) {
+  if (isProtected && request.headers.get("Authorization") !== `Bearer ${token}`) {
     return new Response("Unauthorized", { status: 401, headers: { "WWW-Authenticate": "Bearer" } });
   }
 
@@ -132,7 +128,7 @@ async function handleRequest(request) {
 }
 
 export default {
-  async fetch(request) {
-    return handleRequest(request);
+  async fetch(request, context, env) {
+    return handleRequest(request, env.MCP_AUTH_TOKEN || "");
   },
 };
\ No newline at end of file
